

<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name= »description » content= »La description de page géniale !=)" » />
    <link rel="stylesheet" href="{{ asset('assets/css/styles.css' ) }}" >
    <title>{% block title %}PAGE D'UNE CATEGORIE{% endblock title %}</title>
</head>
<body>


<main>
    <section>

            <p style="color:{{ category.color }}">{{ category.title }}</p>
            {# j'affiche la date de création et publiée en ajoutant le filtre qui affiche correctmt la date  #}
            <p>{{ category.datecreated|date("m/d/Y") }}</p>
            <p>{{ category.datepublished|date("m/d/Y") }}</p>

    </section>

</main>

</body>
</html>
