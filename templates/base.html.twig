<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="description" content="Super page !" />
    <link rel="stylesheet" href="{{ asset('assets/css/styles.css' ) }}" >
    <title>{% block title %}super{% endblock title %}</title>
</head>
<body>
{# création du header avec les liens vers les pages #}
<header>

    {# la création d'une boucle est utile pour afficher plusieurs messages,
    même si ici j'en ai qu'un seul #}
    {# app. une variable globale définie dans TWIG et appelée via l’abstractController #}
    {% for message in app.flashes('success') %}
        <p>{{ message }}</p>
    {% endfor %}


    <div>

    </div>
</header>

{% block main %} {% endblock %}


<footer>

    <div>Les cours avec David c'est de la boulette ! =)</div>
</footer>
</body>
</html>
